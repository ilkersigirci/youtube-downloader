version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - 5000:5000
    # volumes:
    #   - ./backend:/app:Z
  frontend:
    build: ./frontend
    ports:
      - 3000:3000
    # volumes:
    #   - ./frontend:/app:Z
    environment:
      - VITE_BACKEND_URL=http://localhost:5000